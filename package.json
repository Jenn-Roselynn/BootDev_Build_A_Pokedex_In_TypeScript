{
  "name": "bootdev_build_a_pokedex_in_typescript",
  "version": "1.0.0",
  "description": "Build a command-line Pokedex with a REPL, API calls, JSON parsing, and caching in TypeScript.",
  "main": "dist/main.js",
  "type": "module",
  "scripts": {
    "build": "npx tsc",
    "start": "node dist/main.js",
    "dev": "npx tsc && node dist/main.js",
    "test": "vitest --run"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}